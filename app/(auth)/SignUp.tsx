import { FC } from "react";
import { Text, View } from "../../components/Themed";

const SignUp: FC<void> = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Sign up</Text>
    </View>
  );
};

export default SignUp;
